// import { Navigate } from "react-router-dom";
// import { ReactElement } from "react";

// type Props = {
//   children: ReactElement;
//   requiredPermission?: string; // ✅ ajout
// };

// const ProtectedRoute = ({ children, requiredPermission }: Props) => {
//   const token = localStorage.getItem("token");
//   const user = JSON.parse(localStorage.getItem("user") || "null"); // ✅ ajout

//   if (!token) return <Navigate to="/login" replace />;

//   // ✅ Vérif admin
//   if (user?.role === "admin") return children;

//   // ✅ Vérif permission si demandé
//   if (requiredPermission && !user?.permissions?.includes(requiredPermission)) {
//     return <Navigate to="/dashboard" replace />;
//   }

//   return children;
// };

// export default ProtectedRoute;

import { Navigate } from "react-router-dom";
import { ReactElement } from "react";

type Props = {
  children: ReactElement; // ✅ Plus précis que ReactNode
  requiredPermission?: string; 
};

const ProtectedRoute = ({ children, requiredPermission  }: Props) => {
  const token = localStorage.getItem("token");

  if (!token) {
    return <Navigate to="/login" replace />;
  }

  return children;
};

export default ProtectedRoute;
