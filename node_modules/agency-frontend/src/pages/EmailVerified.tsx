import { Link } from "react-router-dom";
import { CheckCircle, ArrowRight, Mail } from "lucide-react";

const EmailVerified: React.FC = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 flex items-center justify-center p-4">
      <div className="relative w-full max-w-md">
        <div className="bg-white rounded-2xl shadow-lg p-8 border border-gray-200 text-center">
          
          {/* Icône de validation */}
          <div className="mx-auto w-20 h-20 rounded-full bg-gray-900 flex items-center justify-center mb-6">
            <CheckCircle className="h-10 w-10 text-white" />
          </div>

          {/* Titre */}
          <h1 className="text-2xl font-bold text-gray-900 mb-4">
            Email vérifié avec succès !
          </h1>

          {/* Sous-titre */}
          <p className="text-gray-600 mb-2 text-lg font-medium">
            Félicitations !
          </p>
          <p className="text-gray-500 mb-8 leading-relaxed">
            Votre adresse email a été confirmée. Vous pouvez maintenant accéder à 
            toutes les fonctionnalités en toute sécurité.
          </p>

          {/* Carte de confirmation */}
          <div className="mb-8 p-4 bg-gray-50 border border-gray-200 rounded-lg">
            <div className="flex items-center justify-center mb-2">
              <Mail className="h-5 w-5 text-gray-600 mr-2" />
              <span className="text-sm font-medium text-gray-800">Email confirmé</span>
            </div>
            <div className="w-full bg-gray-200 rounded-full h-2">
              <div className="bg-gray-700 h-2 rounded-full w-full"></div>
            </div>
          </div>

          {/* Bouton principal */}
          <Link
            to="/login"
            className="inline-flex items-center justify-center w-full bg-gray-900 hover:bg-gray-800 px-8 py-4 rounded-lg text-white font-medium hover:shadow-lg transition-all duration-200 mb-4"
          >
            <span className="mr-3">Se connecter maintenant</span>
            <ArrowRight className="h-5 w-5" />
          </Link>

          {/* Message d'accueil */}
          <div className="p-4 bg-gray-50 border border-gray-200 rounded-lg">
            <p className="text-sm text-gray-800 font-medium">
              Bienvenue dans <span className="font-bold text-gray-900">Agency Connect</span>
            </p>
          </div>

          {/* Lien secondaire */}
          <div className="flex justify-center mt-6 pt-4 border-t border-gray-200">
            <Link 
              to="/register" 
              className="text-sm text-gray-600 hover:text-gray-900 font-medium transition-colors"
            >
              Modifier l'inscription
            </Link>
          </div>
        </div>
      </div>
    </div>
  );
};

export default EmailVerified;